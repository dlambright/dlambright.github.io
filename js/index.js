$(document).ready(function() {
	$('#fullPage').fullpage({
		// autoScrolling:false,
    // lockAnchors: false,
    // anchors:['firstPage', 'secondPage'],
	});

	//methods
	$.fn.fullpage.setAllowScrolling(true);
});
